import React from 'react';

const Returns = (props) => {
    return (
        <div className="view-container">
            <h2>Returns Module</h2>
            <p>This module is currently under development. It will integrate with the new database schemas.</p>
        </div>
    );
};

export default Returns;
